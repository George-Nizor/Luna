{
  "name": "luna",
  "productName": "Luna",
  "version": "0.3.0",
  "description": "A private, local GPU voice studio.",
  "author": "George",
  "private": true,
  "main": "electron/main.cjs",
  "scripts": {
    "start": "electron .",
    "test": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/test.ps1",
    "pack": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build_installer.ps1 -UnpackedOnly",
    "dist": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build_installer.ps1"
  },
  "devDependencies": {
    "electron": "^43.4.0",
    "electron-builder": "^26.15.3"
  }
}
